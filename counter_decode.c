#include "flowradar.h"
#include <stdio.h>

int CounterDecode(struct flowset en_flowset, int *decode_flows,
                  int decode_flows_len, int *b) {

  int a[COUNTINGTABLE_SIZE][decode_flows_len] = {0};

  for (int i = 0; i < decode_flows_len; i++) {
    int flow
  }
}
